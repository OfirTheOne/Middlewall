addos:
  - linux
  - osx

language : node_js

node_js :
 - stable

branches:
  only:
  - production

install:
 - npm ci

script:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then npm run test:osx          ; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then npm run test:lin        ; fi

  